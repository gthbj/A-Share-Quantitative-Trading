# 敏感配置示例文件 —— 复制为 secrets.yaml 并填入真实凭据。
# secrets.yaml 已加入 .gitignore，不会被 Git 跟踪。
#
# 也支持通过环境变量覆盖（优先级高于本文件）：
#   - MAXCOMPUTE_ACCESS_ID
#   - MAXCOMPUTE_ACCESS_KEY

maxcompute:
  access_id: "YOUR_MAXCOMPUTE_ACCESS_ID"
  access_key: "YOUR_MAXCOMPUTE_ACCESS_KEY"
