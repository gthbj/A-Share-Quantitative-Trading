# 项目所有者偏好

## 核心原则

1. 不确定的事情就不必强行完成要求，直接中断输出提问，不要猜测。
2. 没要求的不做。
3. 只改被要求的部分。
4. 给验收标准，别给步骤。
5. 改动代码前先阅读 ARCHITECTURE.md。
6. 修改代码后需修改 ARCHITECTURE.md，保持与代码的一致性。
7. 做需求之前必须先写 PRD。

---

## Git 提交规范

- **Commit 语言**：所有 Commit Message 必须使用**中文**书写。
- **Commit 内容**：简洁描述改动目的，如需列出详细变更项可在消息正文中分点说明。

---

## 需求文档（PRD）书写规范

所有 PRD 文档统一存放于 `PRD/` 目录，文件命名格式为 `PRD_YYYYMMDD_XX_xxxx.md`。

**编号规则**：`XX` 为当日序号，每天从 `01` 重新开始计数。例如 2026-05-09 的文档编号为 `PRD_20260509_01` ~ `PRD_20260509_08`，2026-05-10 的文档编号从 `PRD_20260510_01` 开始。

**命名后缀规则**：文档名末尾需追加 `_xxxx`，`xxxx` 为本文档内容的总结，限制在 20 个汉字以内。例如：`PRD_20260512_13_瓶壁颜色调整与厚度加厚.md`。

### 内容架构要求

PRD 必须包含以下章节，按顺序书写：

1. **元信息**  
   以表格形式列出：LLM型号、输出时间戳（格式：`YYYY-MM-DD HH:MM:SS`）、文档编号、关联 Commit、需求优先级。

2. **问题描述（或背景与动机）**  
   - **Bug 修复类需求**：清楚说明当前存在的问题现象、影响范围，以及期望的行为是什么。**必须包含**：可验证的最小复现步骤（从打开项目/运行到观察到问题的具体操作路径，让任何人都能按步骤 100% 复现）。
   - **新增功能类需求**：改为说明功能背景与动机，阐述为什么需要该功能、不实现的后果是什么、对策略回测或交易行为会产生什么影响。

3. **影响模块声明**  
   明确声明本需求影响哪些模块（`data_layer` / `engine` / `account` / `strategy` / `analytics` / `config` / `utils` / `CLI`），以及是否影响回测结果的可复现性。此节帮助开发者快速判断改动边界。

4. **关键文件路径与现有函数签名**  
   列出需求涉及的核心文件路径，并直接粘贴相关方法的**当前完整签名与关键代码片段**（不要只写方法名）。  
   目的是让后续开发者无需翻找源码即可知道改动的上下文和接口约定。  
   示例：
   ```python
   # strategy/base_strategy.py
   def handle_data(self, context: Context, data: Dict[str, pd.Series]) -> None:
       """核心交易逻辑，每个 Bar 调用一次。"""
       raise NotImplementedError
   ```

5. **需求详情**
   - **功能目标**：具体要做什么，列出功能点。
   - **交互流程**：用文字箭头图或步骤说明用户操作与系统响应的完整流程。量化场景下，需明确信号触发时机、成交价格规则、数据依赖关系。

6. **配置变更（如适用）**  
   若涉及 `config/backtest.yaml` 或类似配置文件的增删改，明确列出：新增/修改的配置项名称、类型、默认值、作用说明。

7. **不可改动的红线区域**  
   明确列出在本次需求中**绝对不能修改**的内容，包括：
   - 不可改动的文件/类/方法
   - 不可变更的接口签名（输入参数、返回值、访问修饰符）
   - 不可变更的数据格式或协议
   - 不可触碰的第三方依赖或插件代码  
   - 不可破坏的回测可复现性（相同策略、参数、数据，多次运行结果一致）  
   目的是防止为修复 A 问题而意外破坏 B 功能。

8. **修改范围与位置**
   - **主要修改文件**：列出需要改动的文件，说明在哪个方法/位置修改，以及修改内容。
   - **不修改的文件**：明确排除范围，避免过度修改。

9. **验收标准**  
   以条目化形式列出可验证的验收条件，用于确认需求是否完成。  
   **必须包含**：
   - 测试用例预期输入输出——至少提供 1~3 组典型的输入/输出示例，用具体数据说明修改前后行为的变化。
   - 回测验证要求——若涉及策略行为变更，需提供一组明确的回测参数（策略、区间、初始资金），并说明预期回测结果的变化方向（如：最大回撤应降低、交易次数应增加等）。  
   示例：
   ```
   用例 1：止损触发场景
   - 输入：持仓 1000 股，成本价 10.00，当前价 9.40，止损阈值 5%
   - 修改前行为：无止损机制，继续持仓
   - 修改后预期行为：触发止损，生成卖出 1000 股的市价单
   ```

10. **备注**  
    补充说明、风险提示、历史背景或待决策事项。量化场景下，需特别关注是否存在 Lookahead Bias（未来函数）风险、过拟合风险、滑点/费用对策略的影响等。
