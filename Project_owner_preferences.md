# 项目所有者偏好

## 核心原则

1. 不确定的事情就不必强行完成要求，直接中断输出提问，不要猜测。
2. 没要求的不做。
3. 只改被要求的部分。
4. 给验收标准，别给步骤。
5. 改动代码前先阅读 ARCHITECTURE.md。

---

## Git 提交规范

- **Commit 语言**：所有 Commit Message 必须使用**中文**书写。
- **Commit 内容**：简洁描述改动目的，如需列出详细变更项可在消息正文中分点说明。

---

## 需求文档（PRD）书写规范

所有 PRD 文档统一存放于 `PRD/` 目录，文件命名格式为 `PRD_YYYYMMDD_XX.md`。

**编号规则**：`XX` 为当日序号，每天从 `01` 重新开始计数。例如 2026-05-09 的文档编号为 `PRD_20260509_01` ~ `PRD_20260509_08`，2026-05-10 的文档编号从 `PRD_20260510_01` 开始。

### 内容架构要求

PRD 必须包含以下章节，按顺序书写：

1. **元信息**  
   以表格形式列出：输出模型、输出时间戳、文档编号、关联 Commit、需求优先级。

2. **问题描述**  
   清楚说明当前存在的问题现象、影响范围，以及期望的行为是什么。  
   **必须包含**：可验证的最小复现步骤（从打开项目/运行游戏到观察到问题的具体操作路径，让任何人都能按步骤 100% 复现）。

3. **关键文件路径与现有函数签名**  
   列出需求涉及的核心文件路径，并直接粘贴相关方法的**当前完整签名与关键代码片段**（不要只写方法名）。  
   目的是让后续开发者无需翻找源码即可知道改动的上下文和接口约定。  
   示例：
   ```csharp
   // Assets/_Project/Scripts/Managers/LevelManager.cs
   private LevelData GenerateProceduralLevel(int levelId)
   {
       int colorCount = Mathf.Min(2 + (levelId / 5), GameColors.MAX_COLOR_ID);
       // ...
   }
   ```

4. **需求详情**
   - **功能目标**：具体要做什么，列出功能点。
   - **交互流程**：用文字箭头图或步骤说明用户操作与系统响应的完整流程。

5. **不可改动的红线区域**  
   明确列出在本次需求中**绝对不能修改**的内容，包括：
   - 不可改动的文件/类/方法
   - 不可变更的接口签名（输入参数、返回值、访问修饰符）
   - 不可变更的数据格式或协议
   - 不可触碰的第三方依赖或插件代码  
   目的是防止为修复 A 问题而意外破坏 B 功能。

6. **修改范围与位置**
   - **主要修改文件**：列出需要改动的文件，说明在哪个方法/位置修改，以及修改内容。
   - **不修改的文件**：明确排除范围，避免过度修改。

7. **验收标准**  
   以条目化形式列出可验证的验收条件，用于确认需求是否完成。  
   **必须包含**：测试用例预期输入输出——至少提供 1~3 组典型的输入/输出示例，用具体数据说明修改前后行为的变化。  
   示例：
   ```
   用例 1：Level 1 运行时生成
   - 输入：levelId = 1, colorCount = 2, emptyCount = 2
   - 修改前输出：bottles = [[1,1,1,1], [2,2,2,2], [], []]（每个满瓶均为单色）
   - 修改后预期输出：bottles 中不存在任何一个纯单色满瓶，且 CheckWin() == false
   ```

8. **备注**  
   补充说明、风险提示、历史背景或待决策事项。
