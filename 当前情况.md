# 项目当前情况梳理

## 一、本次对话完成的工作

### 1. Tushare 数据源修复
- **问题**：`TushareDataSource` 因未实现 `get_stock_list` 和 `get_index_constituents` 两个抽象方法，导致实例化时抛出 `TypeError: Can't instantiate abstract class TushareDataSource with abstract methods get_index_constituents, get_stock_list`。
- **修复**：在 `data_layer/tushare_source.py` 中补充了这两个方法的实现：
  - `get_stock_list()`：调用 `ts.stock_basic()` 获取上市股票列表
  - `get_index_constituents(index_code)`：调用 `ts.index_weight()` 获取指数成分股

### 2. Tushare 可用性排查
- **发现**：修复抽象方法后运行回测，出现大量 `Tushare 数据获取失败: 请指定正确的接口名` 错误。
- **根因**：进一步排查发现，用户提供的 Tushare Token 积分过低，连最基础的 `daily`（股票日线）、`fund_daily`（基金日线）、`trade_cal`（交易日历）等行情接口均无访问权限。仅 `stock_basic`（股票基础信息）等极少数免费接口可用。
- **结论**：Tushare Pro 对该账号而言无法获取 K 线行情数据，暂不具备作为行情数据源的条件。

### 3. AKShare / EastMoney 恢复验证
- **发现**：作为对比，顺手测试了 `akshare.fund_etf_hist_em()`（EastMoney 接口），发现此前因频繁请求导致的 IP Ban 已自动解除，AKShare 可正常拉取 510300.SH 日线数据。
- **结论**：AKShare 已恢复正常，当前应继续使用 AKShare 作为主要数据源。

---

## 二、当前配置状态

### 数据源配置（`config/backtest.yaml`）
```yaml
data:
  source: "tushare"         # 当前仍为 tushare（因用户未要求切回）
  tushare_token: "9dd1e2dea4a9638565e54b77344a4f5bf50b1bd205892c9f080dad99"
```

> **建议**：由于 Tushare 积分不足无法拉取行情，建议将 `source` 改回 `"akshare"` 继续使用 AKShare。

---

## 三、已完成的历史功能（本次对话之前）

| 功能 | 状态 | 关键文件 |
|------|------|----------|
| 双均线策略（日线） | ✅ 可用 | `strategy/double_ma.py` |
| 双均线策略（分钟级） | ✅ 可用 | `strategy/intraday_double_ma.py` |
| 全局止损机制 | ✅ 可用 | `engine/backtest.py` (`_check_stop_loss`) |
| 分钟级回测引擎 | ✅ 可用 | `engine/backtest.py` (`_run_intraday`) |
| ETF/指数数据接入 | ✅ 可用 | `data_layer/akshare_source.py` (`_fetch_etf_bars`) |
| 日期解析 Bug 修复 | ✅ 已修复 | `engine/backtest.py` (`_build_result_df`) |
| 缓存增量更新 | ✅ 可用 | `data_layer/akshare_source.py` + `local_storage.py` |
| 多数据源 Fallback | ⚠️ Tushare 不可用 | `run_backtest.py` (`create_data_source`) |

---

## 四、已知问题与待办

### P0（阻塞/严重）
- [ ] **Tushare 积分不足**：Token 无行情接口权限，Tushare 数据源无法实际使用。如需使用，需前往 https://tushare.pro/document/1?doc_id=108 提升积分。
- [ ] **数据源建议切回 AKShare**：当前 `config/backtest.yaml` 配置为 `tushare`，但运行会失败，需手动改回 `akshare`。

### P1（重要）
- [ ] **止损 lookahead bias（P0-2）**：当前止损逻辑在盘中检查当日最低价并立即触发，存在 lookahead bias。正确做法应是在收盘后检查，次日开盘执行。
- [ ] **CJK 字体警告**：Matplotlib 图表中文显示为方块，需配置中文字体。

### P2（一般）
- [ ] **缓存文件缺失**：此前删除的 `510300.SH` parquet 缓存需重新通过 AKShare 拉取。
- [ ] **Tushare 数据源完整性**：Tushare 源已补充抽象方法实现，但受限于积分尚未跑通全链路回测。

---

## 五、下一步行动建议

1. **立即**：将 `config/backtest.yaml` 中的 `data.source` 改回 `"akshare"`，运行回测验证全链路正常。
2. **短期**：修复止损 lookahead bias（参考 `TODO.md` 中的 P0-2 记录）。
3. **中期**：如需使用 Tushare，先完成积分积累（或购买权限），再验证 Tushare 全链路。
4. **可选**：为 `TushareDataSource` 添加更完善的错误提示（如检测到积分不足时主动提示用户）。

---

*本文档生成时间：2026-05-10*
