# A股模拟量化交易系统需求文档

> **文档状态**：初稿  
> **版本**：v1.0  
> **目标**：搭建一套纯模拟、无需接入真实券商API的A股量化回测与模拟交易框架，支持策略研发、历史回测、虚拟盘跟踪与绩效评估。

---

## 1. 项目概述

### 1.1 背景与目标

随着A股市场的日益机构化，个人投资者需要一套低成本、可复现的量化策略验证工具。本系统完全基于**历史数据与模拟撮合**，不接入任何真实交易通道，核心目标包括：

- **策略研发**：提供简洁的API，支持快速编写和调试选股/择时策略。
- **历史回测**：基于真实历史行情进行高保真回测，严格模拟A股交易规则（T+1、涨跌停、手续费等）。
- **虚拟盘（Paper Trading）**：以每日收盘价或实时快照模拟持仓，跟踪策略在最新市场中的表现。
- **绩效归因**：计算收益率、风险指标、超额收益等，辅助策略迭代。

### 1.2 非真实交易声明

> ⚠️ **本系统仅用于模拟与学术研究，不产生任何真实委托，不涉及资金划转。** 所有成交均为程序内部撮合，价格来源于历史行情或用户导入的数据。

---

## 2. 核心术语

| 术语 | 定义 |
|------|------|
| **Bar** | K线数据，最低支持1分钟级，回测默认使用日线。 |
| **Universe** | 策略可交易的股票池，如沪深300成分股、全A股等。 |
| **Benchmark** | 业绩比较基准，默认使用沪深300指数（000300.SH）。 |
| **Slippage** | 滑点，模拟实际交易中成交价与信号价的偏差。 |
| **Commission** | 交易佣金与税费，包括券商佣金、印花税、过户费。 |
| **Position** | 持仓，记录个股持有数量、成本价、可卖数量（受T+1限制）。 |
| **Order** | 订单，由策略生成的买卖指令。 |
| **Fill** | 成交，由交易引擎根据市场数据模拟撮合的结果。 |

---

## 3. 功能需求

### 3.1 数据层（Data Layer）

#### 3.1.1 数据获取
- **FR-01** 支持从免费/低成本数据源获取A股历史行情，优先支持：
  - `AKShare`（Python开源库，无需账号）
  - `Tushare`（需注册Token，支持更稳定的数据）
  - 本地CSV/Parquet文件导入
- **FR-02** 获取的数据类型包括：
  - 日K线（开、高、低、收、成交量、成交额）
  - 分钟K线（可选，用于日内策略）
  - 复权因子（前复权/后复权切换）
  - 基础信息（股票名称、上市日期、行业、ST标记）
- **FR-03** 支持增量更新：仅下载本地缺失的数据段，避免重复拉取。
- **FR-04** 数据落盘为本地格式（推荐`Parquet`或`SQLite`），支持按日期、股票代码快速索引。

#### 3.1.2 数据管理
- **FR-05** 提供数据质量检查：自动识别停牌、价格异常（如收盘价为0）、缺失交易日。
- **FR-06** 支持数据对齐：回测时自动剔除当日无交易数据的股票，保证Universe内股票均可交易。

---

### 3.2 账户与资产（Account Layer）

#### 3.2.1 账户结构
- **FR-07** 每个策略回测对应一个独立虚拟账户，初始资金可配置（默认100万人民币）。
- **FR-08** 账户状态实时维护：
  - **总资产** = 可用现金 + 所有持仓市值（按最新收盘价）
  - **可用现金**：扣减冻结资金后的可下单金额
  - **持仓市值**：Σ(持仓数量 × 当前收盘价)
  - **冻结资金**：已下单但未成交的金额（开盘竞价阶段）

#### 3.2.2 持仓管理
- **FR-09** 持仓记录需包含：
  - 股票代码、持仓数量、当前成本价（支持先进先出或加权平均）
  - **可卖数量**：严格遵循A股**T+1**制度，当日买入股票不可卖出。
  - 买入日期、盈亏比例
- **FR-10** 支持分红送转的模拟处理（可选）：若回测期间发生分红，持仓数量与成本价自动调整。

---

### 3.3 交易引擎（Execution Engine）

#### 3.3.1 订单类型
- **FR-11** 支持基础订单类型：
  - **市价单（Market Order）**：以当日开盘价或收盘价成交（回测常用）。
  - **限价单（Limit Order）**：指定价格，仅在价格优于或等于指定价时成交。
  - **止损/止盈单（Stop Order）**：触发价到达后转为市价单（可选）。
- **FR-12** 订单方向：买入（Buy）、卖出（Sell）。
- **FR-13** 订单数量必须为**100的整数倍**（A股最小交易单位）。

#### 3.3.2 撮合规则
- **FR-14** 回测撮合模式（默认使用**日K线回测**）：
  - 策略在`t日`收盘后生成信号，系统在`t+1日`以**开盘价**撮合成交。
  - 或：策略在`t日`盘中生成信号，以`t日`收盘价成交（需注明 lookahead bias 风险）。
- **FR-15** 严格模拟涨跌停限制：
  - 若股票当日**涨停**（涨幅≥10%或20%），市价买入单**无法成交**。
  - 若股票当日**跌停**，市价卖出单**无法成交**。
  - 涨停/跌停判定需基于昨日收盘价与当日开/收盘价。
- **FR-16** 滑点模型：默认按成交价附加固定滑点（如0.01元或0.1%），可配置为百分比滑点或固定金额滑点。
- **FR-17** 成交量限制：单笔订单成交量不得超过当日该股票总成交量的特定比例（如10%），防止 unrealistic fill。

#### 3.3.3 交易成本
- **FR-18** 默认交易成本模型：
  - **券商佣金**：买入/卖出均收取，默认万分之2.5，最低5元。
  - **印花税**：卖出时收取，税率千分之0.5（2023年后标准）。
  - **过户费**：沪市股票收取，默认万分之0.1（买卖双向）。
- **FR-19** 交易成本在成交时即时扣减，影响可用现金。

---

### 3.4 策略层（Strategy Layer）

#### 3.4.1 策略接口
- **FR-20** 提供基类 `BaseStrategy`，用户通过继承实现自定义策略：
  - `initialize(context)`：初始化，设置Universe、Benchmark、调度周期等。
  - `before_trading_start(context, data)`：每日开盘前调用，可用于获取宏观数据或预处理。
  - `handle_data(context, data)`：核心逻辑，每个Bar调用一次，生成交易信号。
  - `after_trading_end(context, data)`：每日收盘后调用，可用于日志记录或风险检查。
- **FR-21** `context` 对象暴露以下属性/方法：
  - `context.portfolio`：当前账户资产与持仓。
  - `context.current_dt`：当前回测日期时间。
  - `context.order(stock, amount)`：下市价单，`amount`为正买入、负卖出。
  - `context.order_target_percent(stock, percent)`：调仓至目标仓位（如0.1表示10%资金）。
  - `context.order_target_value(stock, value)`：调仓至目标市值。
  - `context.get_price(stock, count=N)`：获取最近N日行情。

#### 3.4.2 策略示例
系统需内置至少3个示例策略用于验证框架：
- **FR-22** 双均线策略：5日线上穿20日线买入，下穿卖出。
- **FR-23** 动量策略：每月初买入前一个月涨幅最高的10只股票，等权配置。
- **FR-24** 多因子选股策略：基于PE、PB、ROE排序，选取综合评分最高的N只股票。

---

### 3.5 回测引擎（Backtest Engine）

#### 3.5.1 回测流程
- **FR-25** 回测主循环按交易日历逐日推进：
  1. 加载当日所有Universe股票的Bar数据。
  2. 调用 `before_trading_start()`。
  3. 按时间顺序处理日内Bar（日线回测则仅处理一次）。
  4. 调用 `handle_data()`，收集策略生成的订单。
  5. 交易引擎根据当日行情撮合订单，生成交记录。
  6. 更新账户现金与持仓。
  7. 调用 `after_trading_end()`。
  8. 记录当日净值（NAV）。
- **FR-26** 支持回测参数配置：
  - 回测起止日期
  - 初始资金
  - 回测频率（日线、分钟线）
  - 基准指数
  - 滑点与手续费参数

#### 3.5.2 并行回测（可选）
- **FR-27** 支持参数网格搜索：对同一策略的不同参数组合并行回测，加速寻优。
- **FR-28** 支持多进程/多线程回测，利用多核CPU提升效率。

---

### 3.6 虚拟盘（Paper Trading）

- **FR-29** 支持每日定时运行（如收盘后），读取当日行情，模拟策略当日的调仓动作。
- **FR-30** 虚拟盘状态持久化到本地文件（如JSON或SQLite），下次运行时自动加载持仓与资金。
- **FR-31** 生成每日模拟交易报告，包括：信号列表、预计成交、虚拟持仓变动、最新总资产。

---

### 3.7 绩效分析（Performance Analytics）

#### 3.7.1 收益指标
- **FR-32** 计算策略与基准的累计收益率曲线。
- **FR-33** 计算年化收益率（CAGR）。
- **FR-34** 计算超额收益（策略收益 - 基准收益）。

#### 3.7.2 风险指标
- **FR-35** 最大回撤（Max Drawdown）及回撤持续期。
- **FR-36** 波动率（年化标准差）。
- **FR-37** 夏普比率（Sharpe Ratio，无风险利率可配置，默认2%）。
- **FR-38** 信息比率（Information Ratio，相对基准的超额收益稳定性）。
- **FR-39** Beta、Alpha、索提诺比率（Sortino Ratio）。

#### 3.7.3 交易统计
- **FR-40** 统计交易次数、胜率、盈亏比、平均持仓天数、换手率。
- **FR-41** 生成月度/年度收益热力图。

#### 3.7.4 可视化输出
- **FR-42** 自动生成回测报告图表：
  - 策略 vs 基准累计收益对比图
  - 回撤曲线图
  - 月度收益柱状图
  - 持仓权重饼图/柱状图（最新一期）
  - 行业分布图（需结合行业数据）
- **FR-43** 支持导出HTML回测报告，方便分享与存档。

---

## 4. 非功能需求

### 4.1 技术栈
- **NFR-01** 开发语言：**Python 3.10+**
- **NFR-02** 核心依赖：
  - `pandas` / `numpy`：数据处理与数值计算
  - `akshare` / `tushare`：数据获取
  - `matplotlib` / `plotly`：可视化
  - `fastparquet` / `pyarrow`：数据存储（可选）
  - `apscheduler`：虚拟盘定时任务（可选）
  - `streamlit` / `gradio`：简易交互界面（可选）
- **NFR-03** 项目结构清晰，模块化设计，便于扩展与维护。

### 4.2 性能需求
- **NFR-04** 日级回测：对沪深300成分股进行5年回测，耗时不超过**30秒**（普通PC）。
- **NFR-05** 内存管理：单次回测内存占用不超过**2GB**。

### 4.3 可扩展性
- **NFR-06** 策略模块与回测引擎解耦，允许用户在不修改引擎的前提下编写新策略。
- **NFR-07** 数据源可插拔：通过抽象接口 `DataSource` 支持新增数据源（如Wind、自建数据库）。
- **NFR-08** 交易规则可配置：未来如需模拟港股、美股，可通过配置切换规则（如T+0、无涨跌停）。

### 4.4 可靠性
- **NFR-09** 回测结果**100%可复现**：相同策略、相同参数、相同数据，多次运行结果一致。
- **NFR-10** 提供完善的日志记录（`logging`模块），包括数据下载异常、撮合异常、策略运行错误。

### 4.5 易用性
- **NFR-11** 提供命令行入口（CLI），支持一键运行回测：
  ```bash
  python run_backtest.py --strategy my_strategy.py --start 20200101 --end 20231231 --capital 1000000
  ```
- **NFR-12** 提供Jupyter Notebook示例，方便交互式策略开发。
- **NFR-13** 配置与代码分离：交易费率、滑点、初始资金等通过YAML/JSON配置文件管理。

---

## 5. 系统架构

```text
┌─────────────────────────────────────────────────────────────┐
│                     User Strategy (.py)                      │
└───────────────────────┬─────────────────────────────────────┘
                        │
┌───────────────────────▼─────────────────────────────────────┐
│                  Strategy API (BaseStrategy)                 │
└───────────────────────┬─────────────────────────────────────┘
                        │
┌───────────────────────▼─────────────────────────────────────┐
│                Backtest Engine / Paper Trader                │
│         ┌──────────────┐        ┌──────────────┐            │
│         │  Scheduler   │        │  Trade       │            │
│         │  (逐日/逐Bar)│        │  Engine      │            │
│         └──────────────┘        │  (撮合/费用) │            │
│                                 └──────────────┘            │
└───────────────────────┬─────────────────────────────────────┘
                        │
┌───────────────────────▼─────────────────────────────────────┐
│                Data Layer (DataSource)                       │
│         ┌──────────────┐        ┌──────────────┐            │
│         │   AKShare    │        │  Local Store │            │
│         │   Tushare    │        │ (Parquet/DB) │            │
│         └──────────────┘        └──────────────┘            │
└───────────────────────┬─────────────────────────────────────┘
                        │
┌───────────────────────▼─────────────────────────────────────┐
│              Performance & Visualization                     │
│         ┌──────────────┐        ┌──────────────┐            │
│         │  Analytics   │        │   Plotter    │            │
│         │  (Metrics)   │        │ (HTML/PNG)   │            │
│         └──────────────┘        └──────────────┘            │
└─────────────────────────────────────────────────────────────┘
```

---

## 6. 项目目录结构（建议）

```
a_stock_sim_quant/
├── config/
│   └── backtest.yaml          # 回测参数、费率、滑点配置
├── data/
│   ├── raw/                   # 原始下载数据（CSV/Parquet）
│   └── processed/             # 清洗后数据
├── data_layer/
│   ├── __init__.py
│   ├── base_data_source.py    # 数据源抽象基类
│   ├── akshare_source.py      # AKShare实现
│   └── local_storage.py       # 本地数据存取
├── engine/
│   ├── __init__.py
│   ├── backtest.py            # 回测主引擎
│   ├── paper_trader.py        # 虚拟盘
│   └── trade_engine.py        # 撮合与费用计算
├── strategy/
│   ├── __init__.py
│   ├── base_strategy.py       # 策略基类与context
│   ├── double_ma.py           # 双均线示例
│   ├── momentum.py            # 动量示例
│   └── multi_factor.py        # 多因子示例
├── account/
│   ├── __init__.py
│   ├── portfolio.py           # 账户、持仓、资金
│   └── position.py            # 单只股票持仓
├── analytics/
│   ├── __init__.py
│   ├── metrics.py             # 绩效指标计算
│   ├── report.py              # 报告生成
│   └── plotter.py             # 图表绘制
├── utils/
│   ├── __init__.py
│   ├── calendar.py            # A股交易日历
│   └── logger.py              # 日志配置
├── notebooks/
│   └── demo.ipynb             # Jupyter演示
├── run_backtest.py            # CLI入口
├── requirements.txt
└── README.md
```

---

## 7. 里程碑与迭代计划

| 阶段 | 周期 | 交付物 |
|------|------|--------|
| **M1：基础框架** | 1-2周 | 数据层（AKShare）、账户与持仓、简单回测引擎、双均线示例策略。 |
| **M2：交易规则完善** | 1周 | 涨跌停、T+1、滑点、手续费、成交量限制。 |
| **M3：绩效与可视化** | 1周 | 收益率/回撤/夏普计算、Matplotlib图表、HTML报告。 |
| **M4：高级功能** | 1-2周 | 分钟线回测、参数网格搜索、虚拟盘（Paper Trading）、Jupyter/CLI入口。 |
| **M5：优化与文档** | 1周 | 性能优化、完整单元测试、用户文档、示例策略扩展。 |

---

## 8. 风险与约束

1. **数据质量风险**：免费数据源（如AKShare）可能存在延迟或缺失，需建立数据校验机制。
2. **未来函数（Lookahead Bias）**：策略在`t日`收盘前不得使用`t日`收盘后才能知道的信息（如财报数据）。引擎需默认采用`t+1日`开盘价成交，避免此风险。
3. **过拟合风险**：回测表现好不代表实盘表现好，系统仅提供工具，策略有效性由用户自行验证。
4. **版权约束**：若使用Tushare等第三方数据，需遵守其服务条款与调用频率限制。

---

## 9. 附录

### 9.1 A股关键交易规则速查

| 规则 | 说明 |
|------|------|
| 交易时间 | 9:30-11:30, 13:00-15:00 |
| 最小交易单位 | 100股（1手） |
| 涨跌幅限制 | 主板±10%，创业板/科创板±20%，ST股±5% |
| T+1交割 | 当日买入，次日才能卖出 |
| 印花税 | 卖出方承担，0.05% |
| 过户费 | 沪市0.001%双向，深市已合并入佣金 |

### 9.2 参考资源

- [AKShare 文档](https://www.akshare.xyz/)
- [Tushare 官网](https://tushare.pro/)
- 《量化投资：以Python为工具》—— 蔡立耑
- 《主动投资组合管理》—— Grinold & Kahn

---

*文档结束*
